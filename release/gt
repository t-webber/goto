loc="/mnt/d/files/dev/rust/goto/release/goto"
res=$(eval "$loc $1 $2 $3 $4 $5 $6 $7 $8 $9")
if [[ $res == *"#"* ]]; then
    clear=${res:0:1}
    get=${res:2:1}
    path=${res:4}

    if [[ $clear -eq "1" ]]; then
        clear
    fi

    if [[ $get -eq "0" ]]; then
        cd $path
    else
        echo $path
    fi
else 
    echo $res 
fi