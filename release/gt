loc=$(dirname $0)
loc=$loc"/goto"
res=$(eval "$loc $1 $2 $3 $4 $5 $6 $7 $8 $9")
if [[ $res == *"#"* ]]; then
    still=${res:0:1}
    get=${res:2:1}
    path=${res:4}

    if [[ $get == "1" ]]; then
        echo $res[2]
    else
        if [[ $still != "1" ]]; then
            cd $path
        fi
    fi
else
    echo "$res"
fi
